# Паралакс ефект з трьома шарами

Простий паралакс ефект з трьома шарами зображень, які рухаються з різною швидкістю при прокрутці сторінки.

## Структура шарів

### Шар 1 (Передній план)
- **Позиція**: Спереду (z-index: 3)
- **Висота**: 0% - 25% (знизу вгору)
- **Швидкість**: Найшвидший (множник 0.5)
- **Прозорість**: 15%

### Шар 2 (Середній план)
- **Позиція**: Посередині (z-index: 2)
- **Висота**: 15% - 55% (знизу вгору)
- **Швидкість**: Середня (множник 0.3)
- **Прозорість**: 15%

### Шар 3 (Задній план)
- **Позиція**: Позаду (z-index: 1)
- **Висота**: 45% - 100% (знизу вгору)
- **Швидкість**: Найповільніший (множник 0.15)
- **Прозорість**: 15%

## Як використовувати

1. Відкрийте `index.html` у браузері
2. Прокрутіть сторінку вниз/вгору, щоб побачити паралакс ефект
3. Зображення будуть рухатися з різною швидкістю, створюючи ефект глибини

## Налаштування

### Зміна зображень
У файлі `index.html` знайдіть класи `.layer-1`, `.layer-2`, `.layer-3` та змініть значення `background-image`:

```css
.layer-1 {
    background-image: url('ваше-зображення-1.jpg');
}
```

### Зміна прозорості
Змініть значення `opacity` для класу `.parallax-layer`:

```css
.parallax-layer {
    opacity: 0.15; /* Від 0 (повністю прозоре) до 1 (непрозоре) */
}
```

### Зміна швидкості паралаксу
У JavaScript секції змініть множники для кожного шару:

```javascript
layer1.style.transform = `translateY(${relativeScroll * 0.5}px)`; // Змініть 0.5
layer2.style.transform = `translateY(${relativeScroll * 0.3}px)`; // Змініть 0.3
layer3.style.transform = `translateY(${relativeScroll * 0.15}px)`; // Змініть 0.15
```

## Технології
- HTML5
- CSS3
- Vanilla JavaScript

## Сумісність
Працює у всіх сучасних браузерах з підтримкою CSS3 та JavaScript.
